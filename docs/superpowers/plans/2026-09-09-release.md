# Initial Release Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Produce a tested, documented release candidate in the requested GitHub repository and rename the local folder last.

**Architecture:** Keep the current React commands, Fireflies OAuth and Google Calendar confirmation handoff. Replace the locally compiled calendar executable with Raycast's official Swift package bridge, retaining the read-only EventKit behavior.

**Tech Stack:** Raycast API 2, React, TypeScript, Node 22, Swift/EventKit, MCP SDK, Zod.

**Spec:** `docs/superpowers/specs/2026-09-09-release.md`

## Global Constraints

- Raycast 2 on macOS Tahoe (26) or later and Apple Silicon. Verify both native slices without claiming Raycast 1 compatibility.
- US English interface; Polish and English natural-language input.
- No API key setup; preserve existing extension and OAuth identifiers.
- User confirms event creation in the Google Calendar form.
- Do not publish to the Store or change GitHub repository visibility.

## Task 1: Reproducible native distribution

**Files:** `swift/Package.swift`, `swift/Sources/CalendarReader.swift`, `swift/CalendarHelper-Info.plist`, `src/services/calendar-history.ts`, `.gitignore`, `package.json`.

**Interface:** Preserve `calendarStatus()`, `authorizeCalendar()`, and `readHistory(): Promise<CalendarHistory>` for existing commands.

- [x] Verify the old asset contains only the host architecture using `lipo -archs assets/calendar-helper`.
- [x] Move EventKit operations into exported `@raycast` Swift functions with the same JSON models and limits. Pin `extensions-swift-tools` to `1.1.0`.
- [x] Import those functions from `swift:../../swift` and remove the custom build script and committed executable from the release input.
- [x] Run `npm run build`, verify both architectures with `lipo -archs`, verify the embedded calendar usage descriptions, and invoke the status function without requesting new access.

## Task 2: Store interface and release metadata

**Files:** `src/**/*.ts(x)`, `package.json`, `package-lock.json`, `README.md`, `CHANGELOG.md`, `LICENSE`, `.github/workflows/verify.yml`, `docs/RELEASING.md`, `tests/*.test.ts`.

**Interface:** Retain public command IDs and handoff fields; US English display text.

- [x] Translate display strings and errors; add missing placeholders and action icons; keep the prompt capable of understanding Polish.
- [x] Resolve concrete audit findings with a failing regression test before each behavior fix.
- [x] Fill Store author from the user's real Raycast handle, pin installed dependencies, provide MIT license and release notes.
- [x] Document setup, actual data flows, limits, and exact release commands. Add CI to install locked dependencies, lint, typecheck, test and build from Swift sources.
- [ ] Capture native screenshots using synthetic data. Blocked: native UI automation connection failed during preparation.
- [x] Run `npm run verify` and `npm run lint:store` (25 tests passed); verify native distribution and current handoff source contract.
- [ ] Repeat the complete optimized-build flow in Raycast after the native migration. Blocked by unavailable native UI automation; no event was submitted.

## Task 3: Repository and local handoff

**Files:** repository metadata, `docs/RELEASING.md`.

**Interface:** SSH origin `git@github.com:RadnoK/raycast-acal.git`; final local folder `raycast-acal`.

- [ ] Check staged files for secrets, build artifacts and personal meeting data.
- [ ] Commit the complete candidate and push the initial main branch to the empty private repository.
- [ ] Verify the remote commit and CI result; fix failures before completion.
- [ ] Stop the old development watcher, rename the folder from its parent, rebuild/import from the new path, and verify clean Git status and persisted Fireflies access.
- [ ] Record completed validation and any concrete Store submission prerequisites; deliver repository and folder links.
